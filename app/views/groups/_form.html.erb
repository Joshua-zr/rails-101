<%= simple_form_for @group do |f| %>
<div class="form-group">
  <!-- <% if @group.errors.any? %>
    <ul>
      <% @group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  <% end %> -->
  <!-- <div class="form-group string optional">
    <%= f.label "标题", :class => "string optional control-label" %>
    <%= f.text_field :title, :class => "string optional form-control form-control"%>
  </div> -->
  <%= f.input :title, input_html: { class: "form-control" } %>
  <!-- <div class="form-group string optional">
    <%= f.label "描述", :class => "string optional control-label"%>
    <%= f.text_area :description, :class => "string optional form-control form-control" %>
  </div> -->
  <%= f.input :description, input_html: { class: "form-control" }%>
</div>
<%= f.submit "Submit", class: "btn btn-parimary", data: { disable_with: 'Submitting...' } %>
<% end %>
